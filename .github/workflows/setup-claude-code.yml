name: Setup Claude Code on Hetzner
on:
  workflow_dispatch:
jobs:
  setup:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Install Claude Code and Plugins
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.HETZNER_IP }}
          username: root
          password: ${{ secrets.HETZNER_ROOT_PASSWORD }}
          key: ${{ secrets.HETZNER_SSH_KEY }}
          timeout: 120s
          command_timeout: 10m
          script: |
            set -e
            export DEBIAN_FRONTEND=noninteractive
            echo "=== [1/5] Node.js 20 ==="
            if ! command -v node &>/dev/null || [ "$(node -v | cut -d. -f1 | tr -d v)" -lt 20 ]; then
              curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
              apt-get install -y nodejs
            fi
            node -v && npm -v
            echo "=== [2/5] Claude Code CLI ==="
            npm install -g @anthropic-ai/claude-code@latest 2>&1 | tail -5
            echo "=== [3/5] CC Status Line ==="
            npm install -g cc-status-line@latest 2>&1 | tail -5
            mkdir -p /root/.claude
            echo '{"line1":"model | context% | session_cost | session_clock","line2":"git_branch | git_worktree"}' > /root/.claude/cc-status-line.json
            echo "=== [4/5] Context7 Plugin Config ==="
            python3 -c "
import json, os
p = '/root/.claude/settings.json'
s = {}
if os.path.exists(p):
    try:
        with open(p) as f: s = json.load(f)
    except: pass
if 'plugins' not in s: s['plugins'] = []
if not any('context7' in str(x).lower() for x in s.get('plugins',[])):
    s['plugins'].append({'name':'context7','source':'marketplace'})
with open(p,'w') as f: json.dump(s,f,indent=2)
print(json.dumps(s,indent=2))
"
            echo "=== [5/5] Verify ==="
            echo "Node: $(node -v)"
            echo "NPM: $(npm -v)"
            which claude && claude --version || echo "Claude Code installed (run claude to verify)"
            ls -la /root/.claude/
            cat /root/.claude/settings.json
            echo "=== DONE ==="
